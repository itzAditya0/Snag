# OS directory info files
.DS_Store
desktop.ini

# node
node_modules

# static build
build

# secrets
.env
.env.*
!.env.example
cookies.json

# docker
docker-compose.yml

# ide
.vscode

# go binary + go-only subtree (the Dockerfile builds the api only, so
# the cli artifacts are dead weight in the build context)
cli/snag
cli/dist
cli/.snag-cache

# claude code / dev tooling — never goes into a container
.claude
.code-review-graph
.cursor
*.log
